<% if session[:admin_user] == "admin" || session[:admin_user] == "regular_admin" %>
  <!-- 管理者用の店舗一覧 -->
  <%= render 'admin_store_index' %>
<% else %>
  <!-- 一般ユーザー用の店舗一覧 -->
  <%= render 'general_user_store_index' %>
<% end %>
