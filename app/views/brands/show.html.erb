<% if session[:admin_user] == "admin" || session[:admin_user] == "regular_admin" %>
  <!-- 管理者用のブランド詳細 -->
  <%= render 'admin_brand_show' %>
<% else %>
  <!-- 一般ユーザー用のブランド詳細 -->
  <%= render 'general_user_brand_show' %>
<% end %>
