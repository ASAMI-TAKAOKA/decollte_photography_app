<% if session[:admin_user] == "admin" || session[:admin_user] == "regular_admin" %>
  <!-- 管理者用のブランド一覧 -->
  <%= render 'admin_brand_index' %>
<% else %>
  <!-- 一般ユーザー用のブランド一覧 -->
  <%= render 'general_user_brand_index' %>
<% end %>